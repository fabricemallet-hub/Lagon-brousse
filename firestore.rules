rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Insecure rules for debugging deployment conflicts.
    // This is a temporary measure to unblock the server.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
