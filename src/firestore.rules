rules_version = '2';

// Version 8.0.0 - SÉCURITÉ MAÎTRE EXPLICITE
service cloud.firestore {
  match /databases/{database}/documents {

    // --- FONCTION MAÎTRE PRIORITAIRE ---
    function isMaster() {
      return request.auth != null && (
        request.auth.uid == "t8nPnZLcTiaLJSKMuLzib3C5nPn1" ||
        request.auth.uid == "ipupi3Pg4RfrSEpFyT69BtlCdpi2" ||
        request.auth.uid == "Irglq69MasYdNwBmUu8yKvw6h4G2" ||
        (request.auth.token.email != null && request.auth.token.email.lower() == "f.mallet81@outlook.com")
      );
    }

    // --- 1. ACCÈS TOTAL POUR LE MAÎTRE ---
    match /{allPaths=**} {
      allow read, write: if isMaster();
    }

    // --- 2. LISTAGE EXPLICITE DES RACINES (Indispensable pour SDK JS) ---
    match /conversations { allow list: if isMaster(); }
    match /users { allow list: if isMaster(); }
    match /businesses { allow list: if isMaster(); }
    match /access_tokens { allow list: if isMaster(); }
    match /campaigns { allow list: if isMaster(); }
    match /system_notifications { allow list: if isMaster(); }
    match /app_settings { allow list: if isMaster(); }

    // --- 3. FONCTION POUR ADMINS SECONDAIRES & PROS ---
    function isSecondaryAdmin() {
      return request.auth != null && (
        request.auth.uid in ["K9cVYLVUk1NV99YV3anebkugpPp1"] ||
        (request.auth.token.email != null && request.auth.token.email.lower() in [
          "fabrice.mallet@gmail.com", 
          "f.mallet81@gmail.com"
        ])
      );
    }

    // --- 4. RÈGLES POUR UTILISATEURS STANDARDS ---
    
    match /conversations/{userId} {
      allow read, write: if request.auth != null && (request.auth.uid == userId || isSecondaryAdmin());
      match /{allPaths=**} {
        allow read, write: if request.auth != null && (request.auth.uid == userId || isSecondaryAdmin());
      }
    }

    match /users/{userId} {
      allow read, write: if request.auth != null && (request.auth.uid == userId || isSecondaryAdmin());
      match /{allPaths=**} {
        allow read, write: if request.auth != null && (request.auth.uid == userId || isSecondaryAdmin());
      }
    }

    match /app_settings/{id} {
      allow read: if true;
      allow write: if isSecondaryAdmin();
    }

    match /system_notifications/{id} {
      allow read: if true;
      allow write: if isSecondaryAdmin();
    }

    match /meteo_caledonie/{id} {
      allow get, list: if true;
      match /previsions/{prevId} {
        allow get, list: if true;
      }
      allow write: if isSecondaryAdmin();
    }

    match /fish_species/{id} {
      allow get, list: if true;
      allow write: if isSecondaryAdmin();
      match /commune_stats/{communeId} {
        allow get, list: if true;
        allow create, update: if request.auth != null;
      }
    }
    
    match /sound_library/{id} {
      allow get, list: if true;
      allow write: if isSecondaryAdmin();
    }

    match /cms_support/faq/items/{id} {
      allow get, list: if true;
      allow write: if isSecondaryAdmin();
    }

    match /cms_support/tickets/items/{id} {
      allow create: if request.auth != null;
      allow read: if request.auth != null && (resource.data.userId == request.auth.uid || isSecondaryAdmin());
      allow list, update, delete: if isSecondaryAdmin();
    }

    match /hunting_sessions/{id} {
      allow read, write: if request.auth != null;
      match /{allPaths=**} {
        allow read, write: if request.auth != null;
      }
    }

    match /vessels/{id} {
      allow read, write: if request.auth != null;
      match /{allPaths=**} {
        allow read, write: if request.auth != null;
      }
    }

    match /spot_shares/{id} {
      allow create: if request.auth != null;
      allow read, delete: if request.auth != null && (
        isSecondaryAdmin() || 
        (request.auth.token.email != null && resource.data.recipientEmail == request.auth.token.email.lower())
      );
    }

    match /businesses/{businessId} {
      allow get, list: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && (resource.data.ownerId == request.auth.uid || isSecondaryAdmin());

      match /promotions/{promoId} {
        allow get, list: if true;
        allow write: if request.auth != null;
      }
    }

    match /campaigns/{campaignId} {
      allow create: if request.auth != null;
      allow read: if request.auth != null && (resource.data.ownerId == request.auth.uid || isSecondaryAdmin());
    }
  }
}