
{
  "entities": {
    "UserAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserAccount",
      "type": "object",
      "description": "Represents a user account within the application.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the user account." },
        "email": { "type": "string", "description": "The user's email address.", "format": "email" },
        "displayName": { "type": "string", "description": "The user's public display name." },
        "subscriptionStatus": { "type": "string", "description": "The current subscription status of the user (e.g., 'active', 'inactive', 'trial', 'admin')." },
        "subscriptionStartDate": { "type": "string", "description": "The date when the user's subscription started.", "format": "date-time" },
        "subscriptionExpiryDate": { "type": "string", "description": "The date when the user's subscription expires.", "format": "date-time" },
        "favoriteLocationIds": { "type": "array", "description": "References to Location. (Relationship: UserAccount N:N Location)", "items": { "type": "string" } },
        "mapIcon": { "type": "string", "description": "The user's chosen icon for the hunting map." },
        "mapColor": { "type": "string", "description": "The user's chosen color for their map icon." },
        "lastSelectedLocation": { "type": "string", "description": "The last location selected by the user." },
        "emergencyContact": { "type": "string", "description": "The user's saved emergency contact number." }
      },
      "required": ["id", "email", "displayName", "subscriptionStatus"]
    },
    "VesselStatus": {
      "title": "VesselStatus",
      "type": "object",
      "description": "Represents the real-time tracking status of a boat/vessel.",
      "properties": {
        "userId": { "type": "string" },
        "displayName": { "type": "string" },
        "location": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" }
          }
        },
        "status": { "type": "string", "enum": ["moving", "stationary", "offline"] },
        "lastActive": { "type": "string", "format": "date-time" },
        "isSharing": { "type": "boolean" },
        "batteryLevel": { "type": "number" }
      },
      "required": ["userId", "location", "status", "lastActive"]
    },
    "VesselHistory": {
      "title": "VesselHistory",
      "type": "object",
      "description": "Historical stationary points for a vessel.",
      "properties": {
        "timestamp": { "type": "string", "format": "date-time" },
        "location": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" }
          }
        },
        "durationMinutes": { "type": "number" }
      }
    },
    "SowingRecord": {
      "title": "SowingRecord",
      "type": "object",
      "description": "Represents a sowing entry saved by the user.",
      "properties": {
        "userId": { "type": "string" },
        "seedName": { "type": "string" },
        "sowingDate": { "type": "string", "format": "date-time" },
        "plantType": { "type": "string" },
        "cultureAdvice": { "type": "string" },
        "estimatedHarvestDate": { "type": "string" },
        "transplantingAdvice": { "type": "string" },
        "lunarContext": {
          "type": "object",
          "properties": {
            "phase": { "type": "string" },
            "zodiac": { "type": "string" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "seedName", "sowingDate"]
    },
    "Location": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Location",
      "type": "object",
      "description": "Represents a location (commune) selected by the user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the location." },
        "name": { "type": "string", "description": "The name of the location." },
        "latitude": { "type": "number", "description": "The latitude of the location." },
        "longitude": { "type": "number", "description": "The longitude of the location." }
      },
      "required": ["id", "name", "latitude", "longitude"]
    },
    "Station": {
      "title": "Station",
      "type": "object",
      "description": "Represents a tide station with its coordinates.",
      "properties": {
        "name": { "type": "string" },
        "lat": { "type": "number" },
        "lon": { "type": "number" }
      }
    },
    "TideArchive": {
      "title": "TideArchive",
      "type": "object",
      "description": "Contains the tide events for a specific day and station.",
      "properties": {
        "tides": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": { "type": "string" },
              "time": { "type": "string" },
              "height": { "type": "number" }
            },
            "required": ["type", "time", "height"]
          }
        }
      }
    },
    "HuntingSession": {
      "title": "HuntingSession",
      "type": "object",
      "description": "Represents a real-time hunting session.",
      "properties": {
        "organizerId": { "type": "string", "description": "The UID of the user who created the session." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the session was created." },
        "expiresAt": { "type": "string", "format": "date-time", "description": "Timestamp when the session expires and should be deleted by TTL." }
      },
      "required": ["organizerId", "createdAt", "expiresAt"]
    },
    "SessionParticipant": {
      "title": "SessionParticipant",
      "type": "object",
      "description": "Represents a participant in a hunting session.",
      "properties": {
        "displayName": { "type": "string", "description": "The display name of the participant." },
        "mapIcon": { "type": "string", "description": "The participant's chosen icon for the hunting map." },
        "mapColor": { "type": "string", "description": "The participant's chosen color for their map icon." },
        "baseStatus": {
          "type": "string",
          "description": "The participant's main action status.",
          "enum": ["En position", "Battue en cours"]
        },
        "isGibierEnVue": {
            "type": "boolean",
            "description": "Indicates if the participant has game in sight."
        },
        "location": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" }
          }
        },
        "battery": {
          "type": "object",
          "properties": {
            "level": { "type": "number", "description": "Battery level from 0 to 1." },
            "charging": { "type": "boolean", "description": "Is the device charging." }
          }
        },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Last update timestamp." }
      },
      "required": ["displayName", "updatedAt"]
    },
     "AccessToken": {
      "title": "AccessToken",
      "type": "object",
      "description": "Represents a generated access token for a limited-time subscription.",
      "properties": {
        "durationMonths": { "type": "number", "description": "The duration of the subscription in months granted by this token." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the token was created." },
        "status": { "type": "string", "enum": ["active", "redeemed"], "description": "The current status of the token." },
        "redeemedBy": { "type": "string", "description": "The UID of the user who redeemed the token." },
        "redeemedAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the token was redeemed." }
      },
      "required": ["durationMonths", "createdAt", "status"]
    },
    "SharedAccessToken": {
      "title": "SharedAccessToken",
      "type": "object",
      "description": "Represents a globally shared access token that grants access to all users.",
      "properties": {
        "durationMonths": { "type": "number", "description": "The duration of the subscription in months granted by this token." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the token was created." },
        "expiresAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the token expires." }
      },
      "required": ["durationMonths", "createdAt", "expiresAt"]
    },
    "Conversation": {
      "title": "Conversation",
      "type": "object",
      "description": "Represents a single conversation thread between a user and the admin.",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who initiated the conversation." },
        "userEmail": { "type": "string", "description": "The email of the user." },
        "userDisplayName": { "type": "string", "description": "The display name of the user." },
        "lastMessageContent": { "type": "string", "description": "A snippet of the latest message for preview." },
        "lastMessageAt": { "type": "string", "format": "date-time", "description": "Timestamp of the last message sent." },
        "isReadByAdmin": { "type": "boolean", "description": "True if the admin has read the latest message from the user." },
        "isReadByUser": { "type": "boolean", "description": "True if the user has read the latest message from the admin." }
      },
      "required": ["userId", "userEmail", "userDisplayName", "lastMessageAt", "isReadByAdmin", "isReadByUser"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents a single message within a conversation.",
      "properties": {
        "senderId": { "type": "string", "description": "The UID of the sender ('admin' if from the administrator)." },
        "content": { "type": "string", "description": "The text content of the message." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the message was sent." }
      },
      "required": ["senderId", "content", "createdAt"]
    },
    "FishingSpot": {
      "title": "FishingSpot",
      "type": "object",
      "description": "Represents a saved fishing spot with its environmental context.",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who saved the spot." },
        "name": { "type": "string", "description": "A name or comment for the spot." },
        "location": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" }
          }
        },
        "icon": { "type": "string", "description": "The icon chosen for the spot marker." },
        "color": { "type": "string", "description": "The color chosen for the spot marker." },
        "fishingTypes": {
          "type": "array",
          "description": "The fishing techniques used at this spot.",
          "items": { "type": "string" }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "context": {
          "type": "object",
          "description": "Environmental context at the time of saving.",
          "properties": {
            "timestamp": { "type": "string", "format": "date-time" },
            "moonPhase": { "type": "string" },
            "tideHeight": { "type": "number" },
            "tideMovement": { "type": "string" },
            "tideCurrent": { "type": "string" },
            "weatherCondition": { "type": "string" },
            "windSpeed": { "type": "number" },
            "windDirection": { "type": "string" },
            "airTemperature": { "type": "number" },
            "waterTemperature": { "type": "number" }
          }
        }
      },
      "required": ["userId", "name", "location", "createdAt", "context"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserAccount",
          "schema": { "$ref": "#/backend/entities/UserAccount" },
          "description": "Stores user account information.",
          "params": [{ "name": "userId", "description": "The unique identifier for the user account." }]
        }
      },
      {
        "path": "/vessels/{userId}",
        "definition": {
          "entityName": "VesselStatus",
          "schema": { "$ref": "#/backend/entities/VesselStatus" },
          "description": "Real-time vessel status for tracking.",
          "params": [{ "name": "userId", "description": "The UID of the vessel owner." }]
        }
      },
      {
        "path": "/vessels/{userId}/history/{pointId}",
        "definition": {
          "entityName": "VesselHistory",
          "schema": { "$ref": "#/backend/entities/VesselHistory" },
          "description": "Stationary points history.",
          "params": [
            { "name": "userId", "description": "Vessel owner UID." },
            { "name": "pointId", "description": "Point ID." }
          ]
        }
      }
    ]
  }
}
