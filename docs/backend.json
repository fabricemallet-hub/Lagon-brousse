
{
  "entities": {
    "UserAccount": {
      "title": "UserAccount",
      "type": "object",
      "description": "Profil utilisateur avec droits admin et préférences.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "subscriptionStatus": { "type": "string", "enum": ["active", "inactive", "trial", "admin"] },
        "subscriptionExpiryDate": { "type": "string" },
        "lastSelectedLocation": { "type": "string" },
        "emergencyContact": { "type": "string" }
      },
      "required": ["id", "email", "subscriptionStatus"]
    },
    "HuntingSession": {
      "title": "HuntingSession",
      "type": "object",
      "properties": {
        "organizerId": { "type": "string" },
        "createdAt": { "type": "string" },
        "expiresAt": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserAccount" },
      "description": "Données utilisateurs."
    },
    "/hunting_sessions/{sessionId}": {
      "schema": { "$ref": "#/backend/entities/HuntingSession" },
      "description": "Sessions de chasse."
    },
    "/hunting_sessions/{sessionId}/participants/{userId}": {
      "description": "Participants temps réel."
    },
    "/vessels/{vesselId}": {
      "description": "Tracker maritime."
    }
  }
}
